#include <stdio.h>
#include <string.h>

int main()
{
    char alphabet[] = "ABCDEFGHIJKLMNOPQRSTUVWXYZ";
    printf("Length = %d", strlen(alphabet));
    printf("\nSize = %d", sizeof(alphabet));
    return 0;
}