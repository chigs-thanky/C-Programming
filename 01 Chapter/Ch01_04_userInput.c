#include <stdio.h>

int main()
{
    // int a;
    int x;
    int y;
    int z;
    // printf("Enter an integer: ");
    // scanf("%d", &a);
    // printf("Entered value is: %d\n", a);
    printf("Enter an num 1: ");
    scanf("%d", &x);
    printf("Enter an num 2: ");
    scanf("%d", &y);
    z = x + y;
    printf("Sum = %d", z);
    return 0;
}
